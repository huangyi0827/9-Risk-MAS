Evidence rules:
- Evidence must reference a concrete metric or a tool output with provenance.
- Allowed ref prefixes: snapshot_metrics.*, rules.*, tool:*.
- Use refs like snapshot_metrics.* or tool:macro_timeseries.
- Avoid speculative claims without evidence.
